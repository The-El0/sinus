{
  "name": "@the-el0/sinus",
  "version": "0.0.1",
  "description": "Sinus",
  "author": "the-el0@protonmail.com",
  "repository": {
    "type": "git",
    "url": "git@github.com:The-El0/sinus.git"
  },
  "private": "false",
  "engines": {
    "node": ">= 10.16.0",
    "npm": ">= 6.9.0"
  },
  "scripts": {
    "sass": "node-sass --watch src/styles -o build --output-style compressed -q",
    "build": "node-sass --include-path scss src/styles/app.scss build/app.css"
  },
  "dependencies": {
    "node-sass": "^4.12.0",
    "normalize.css": "^8.0.1"
  }
}
